pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                echo 'Building from Jenkinsfile'
            }
        }
        stage('Deploy') {
            steps {
                echo 'Deploying from Jenkinsfile'
            }
        }
        stage('Test') {
            steps {
                echo 'Testing from Jenkinsfile'
            }
        }
        stage('Release') {
            steps {
                echo 'Releasing from Jenkinsfile'
            }
        }
    }
}
